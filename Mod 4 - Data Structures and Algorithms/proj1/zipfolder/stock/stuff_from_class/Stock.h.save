#ifndef STOCK_H
#define STOCK_H

#include <iostream>
#include <fstream>
#include <iomanip>
#include <string>

//use project 3 from mod 2 as example
class Stock{

public:
    explicit Stock(string = "null", string = "nul", double = 0.0, int = 0);
    /*Stock(Stock other); //Delete this?
    string getKey();
    string getName();
    string getTicker();
    double getPrice();
    int getQuantity();
    void setTicker(string);
    void setName(string);
    void setPrice(double);
    void setQuantity(int);*/

private:
    string ticker;
    string name;
    double price;
    int quantity;

};

#endif
